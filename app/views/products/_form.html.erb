<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :shopify_product_id %>
    <%= form.text_field :shopify_product_id %>
  </div>

  <div class="field">
    <%= form.label :shopify_handle %>
    <%= form.text_field :shopify_handle %>
  </div>

  <div class="field">
    <%= form.label :sme_commission %>
    <%= form.text_field :sme_commission %>
  </div>

  <div class="field">
    <%= form.label :shopify_product_data %>
    <%= form.text_field :shopify_product_data %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
